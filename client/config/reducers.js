import {combineReducers} from 'redux';
const newState = (state = {}, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
//Reducer合并
export default combineReducers({newState});
